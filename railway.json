{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm i && cd backend && npx prisma generate && cd .. && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy && cd .. && node backend/dist/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}